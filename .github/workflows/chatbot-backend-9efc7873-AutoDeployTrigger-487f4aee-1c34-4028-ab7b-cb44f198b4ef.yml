name: Trigger auto deployment for chatbot-backend-9efc7873

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ stage-10 ]
    paths:
    - '**'
    - '.github/workflows/chatbot-backend-9efc7873-AutoDeployTrigger-487f4aee-1c34-4028-ab7b-cb44f198b4ef.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CHATBOTBACKEND9EFC7873_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CHATBOTBACKEND9EFC7873_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CHATBOTBACKEND9EFC7873_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          registryUrl: acr9efc7873.azurecr.io
          registryUsername: ${{ secrets.CHATBOTBACKEND9EFC7873_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CHATBOTBACKEND9EFC7873_REGISTRY_PASSWORD }}
          containerAppName: chatbot-backend-9efc7873
          resourceGroup: stage-10
          imageToBuild: acr9efc7873.azurecr.io/chatbot-backend-9efc7873:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


