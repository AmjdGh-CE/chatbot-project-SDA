name: Trigger auto deployment for chatbot-backend-6d7a8ebb

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ stage-10 ]
    paths:
    - '**'
    - '.github/workflows/chatbot-backend-6d7a8ebb-AutoDeployTrigger-94cba25f-88e5-43ea-a207-75be93af0586.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CHATBOTBACKEND6D7A8EBB_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CHATBOTBACKEND6D7A8EBB_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CHATBOTBACKEND6D7A8EBB_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          registryUrl: acr6d7a8ebb.azurecr.io
          registryUsername: ${{ secrets.CHATBOTBACKEND6D7A8EBB_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CHATBOTBACKEND6D7A8EBB_REGISTRY_PASSWORD }}
          containerAppName: chatbot-backend-6d7a8ebb
          resourceGroup: stage-10
          imageToBuild: acr6d7a8ebb.azurecr.io/chatbot-backend-6d7a8ebb:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


