name: Trigger auto deployment for chatbot-backend-7ede838a

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ stage-10 ]
    paths:
    - '**'
    - '.github/workflows/chatbot-backend-7ede838a-AutoDeployTrigger-b99f9d2d-4eba-40d8-a7cf-6af9250b6ad0.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CHATBOTBACKEND7EDE838A_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CHATBOTBACKEND7EDE838A_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CHATBOTBACKEND7EDE838A_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          registryUrl: acr7ede838a.azurecr.io
          registryUsername: ${{ secrets.CHATBOTBACKEND7EDE838A_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CHATBOTBACKEND7EDE838A_REGISTRY_PASSWORD }}
          containerAppName: chatbot-backend-7ede838a
          resourceGroup: stage-10
          imageToBuild: acr7ede838a.azurecr.io/chatbot-backend-7ede838a:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


