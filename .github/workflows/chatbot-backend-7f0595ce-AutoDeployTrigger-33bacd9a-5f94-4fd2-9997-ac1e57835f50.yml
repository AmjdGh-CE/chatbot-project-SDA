name: Trigger auto deployment for chatbot-backend-7f0595ce

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ stage-10 ]
    paths:
    - '**'
    - '.github/workflows/chatbot-backend-7f0595ce-AutoDeployTrigger-33bacd9a-5f94-4fd2-9997-ac1e57835f50.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CHATBOTBACKEND7F0595CE_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CHATBOTBACKEND7F0595CE_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CHATBOTBACKEND7F0595CE_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          registryUrl: acr7f0595ce.azurecr.io
          registryUsername: ${{ secrets.CHATBOTBACKEND7F0595CE_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CHATBOTBACKEND7F0595CE_REGISTRY_PASSWORD }}
          containerAppName: chatbot-backend-7f0595ce
          resourceGroup: stage-10
          imageToBuild: acr7f0595ce.azurecr.io/chatbot-backend-7f0595ce:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


