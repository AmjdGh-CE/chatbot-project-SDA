name: Trigger auto deployment for chatbot-backend-bd44b8c1

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ stage-10 ]
    paths:
    - '**'
    - '.github/workflows/chatbot-backend-bd44b8c1-AutoDeployTrigger-fa708036-923b-4ea8-a1d8-30f914a188d0.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CHATBOTBACKENDBD44B8C1_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CHATBOTBACKENDBD44B8C1_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CHATBOTBACKENDBD44B8C1_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          registryUrl: acrbd44b8c1.azurecr.io
          registryUsername: ${{ secrets.CHATBOTBACKENDBD44B8C1_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CHATBOTBACKENDBD44B8C1_REGISTRY_PASSWORD }}
          containerAppName: chatbot-backend-bd44b8c1
          resourceGroup: stage-ACA
          imageToBuild: acrbd44b8c1.azurecr.io/chatbot-backend-bd44b8c1:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


